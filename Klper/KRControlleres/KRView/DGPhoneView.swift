
import Foundation

import UIKit

protocol FIDZJPhoneAnima: AnyObject {
    func elevtCardViewPresent()
}

class DGPhoneView: UIView {
var statePadding: Float = 0.0
private var creat_padding: Float = 0.0



    
    weak var dataSource: FIDZJPhoneAnima?

@discardableResult
 func aspectStateConnectionChatRemain(creationPicture: [String: Any]!) -> Double {
    var animao: Bool = false
    var handlerW: Double = 5.0
   withUnsafeMutablePointer(to: &handlerW) { pointer in
          _ = pointer.pointee
   }
      handlerW *= (Double((animao ? 3 : 1) >> (Swift.min(labs(Int(handlerW > 282906984.0 || handlerW < -282906984.0 ? 58.0 : handlerW)), 4))))
   repeat {
      handlerW -= (Double((animao ? 1 : 1) - Int(handlerW > 227146421.0 || handlerW < -227146421.0 ? 9.0 : handlerW)))
      if 680672.0 == handlerW {
         break
      }
   } while (680672.0 == handlerW) && (!animao)
      animao = handlerW == 91.89
     let responseQheader: Double = 6812.0
    var qsvscaleBenchs:Double = 0
    qsvscaleBenchs /= Swift.max(responseQheader, 1)
         var _y_66 = Int(responseQheader)
     if _y_66 > 934 {
          _y_66 *= 43
          _y_66 /= 69
     }

    return qsvscaleBenchs

}






    @IBAction func upgrades(_ sender: Any) {

         let nofiticationCompose: Double = aspectStateConnectionChatRemain(creationPicture:[String(cString: [97,114,107,101,114,95,110,95,56,51,0], encoding: .utf8)!:431, String(cString: [120,118,109,99,0], encoding: .utf8)!:754, String(cString: [115,95,57,56,95,101,110,99,114,121,112,116,105,111,110,0], encoding: .utf8)!:722])

      if nofiticationCompose != 55 {
             print(nofiticationCompose)
      }
     var z_4 = Int(nofiticationCompose)
     var v_25 = 1
     let y_24 = 1
     if z_4 > y_24 {
         z_4 = y_24
     }
     while v_25 < z_4 {
         v_25 += 1
          z_4 += v_25
         break
     }

_ = nofiticationCompose


       var contQ: String! = String(cString: [118,101,114,105,102,105,99,97,116,105,111,110,95,119,95,48,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &contQ) { pointer in
          _ = pointer.pointee
   }
    var weixinlabelF: Bool = false
    var scroll6: Bool = false
    _ = scroll6
   while (1 <= contQ.count && !weixinlabelF) {
      contQ = "\(((weixinlabelF ? 5 : 3)))"
      break
   }

   for _ in 0 ..< 2 {
       var alabel5: String! = String(cString: [106,95,53,50,95,99,108,105,101,110,116,0], encoding: .utf8)!
          var timer9: String! = String(cString: [115,97,117,99,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &timer9) { pointer in
                _ = pointer.pointee
         }
          var editB: String! = String(cString: [114,101,112,108,97,99,101,100,95,49,95,51,52,0], encoding: .utf8)!
          var labeld: Bool = true
         withUnsafeMutablePointer(to: &labeld) { pointer in
                _ = pointer.pointee
         }
         alabel5.append("\(1 ^ timer9.count)")
         editB = "\(3 * alabel5.count)"
         labeld = (alabel5.count % (Swift.max(7, timer9.count))) == 69
         alabel5 = "\(3 & alabel5.count)"
      while (alabel5.count <= alabel5.count) {
         alabel5 = "\(alabel5.count - alabel5.count)"
         break
      }
      weixinlabelF = !contQ.hasPrefix("\(weixinlabelF)")
   }
        self.dataSource?.elevtCardViewPresent()
      weixinlabelF = (54 >= ((!weixinlabelF ? 54 : contQ.count) >> (Swift.min(contQ.count, 2))))
        
        transform = CGAffineTransformMakeScale(1.0, 1.0)
   repeat {
      weixinlabelF = !scroll6
      if weixinlabelF ? !weixinlabelF : weixinlabelF {
         break
      }
   } while (!scroll6 || !weixinlabelF) && (weixinlabelF ? !weixinlabelF : weixinlabelF)
        alpha = 1.0
       var tabbark: String! = String(cString: [97,110,99,104,111,114,0], encoding: .utf8)!
       _ = tabbark
       var speedsG: Double = 4.0
       var placey: String! = String(cString: [99,104,117,110,107,95,109,95,54,55,0], encoding: .utf8)!
      if (Double(tabbark.count) * speedsG) >= 3.47 || 4 >= (4 * Int(speedsG > 238692625.0 || speedsG < -238692625.0 ? 52.0 : speedsG)) {
         tabbark = "\((placey.count + Int(speedsG > 242828473.0 || speedsG < -242828473.0 ? 38.0 : speedsG)))"
      }
         tabbark.append("\(1)")
         speedsG *= (Double(tabbark == (String(cString:[50,0], encoding: .utf8)!) ? Int(speedsG > 70539275.0 || speedsG < -70539275.0 ? 30.0 : speedsG) : tabbark.count))
       var tabbarq: String! = String(cString: [114,101,115,0], encoding: .utf8)!
       var meal4: String! = String(cString: [97,102,102,105,110,105,116,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &meal4) { pointer in
             _ = pointer.pointee
      }
         placey = "\(tabbark.count ^ 1)"
         tabbark.append("\(2)")
      while (5 == placey.count) {
         placey = "\(tabbark.count / 2)"
         break
      }
      if (1.74 * speedsG) <= 4.5 || (speedsG * Double(placey.count)) <= 1.74 {
          var actionH: Int = 5
          var detecto: String! = String(cString: [109,105,114,114,111,114,105,110,103,0], encoding: .utf8)!
          _ = detecto
          var scaled: Double = 1.0
          var timerf: [String: Any]! = [String(cString: [99,105,114,99,117,108,97,116,105,110,103,0], encoding: .utf8)!:340, String(cString: [120,95,57,52,95,99,104,101,98,121,115,104,101,118,0], encoding: .utf8)!:197]
         speedsG /= Swift.max((Double(detecto == (String(cString:[66,0], encoding: .utf8)!) ? detecto.count : Int(speedsG > 244266926.0 || speedsG < -244266926.0 ? 56.0 : speedsG))), 3)
         actionH += placey.count ^ 3
         scaled += Double(2 ^ actionH)
         timerf["\(tabbark)"] = 2
      }
      while (!tabbark.hasPrefix("\(speedsG)")) {
         tabbark.append("\((Int(speedsG > 16266344.0 || speedsG < -16266344.0 ? 38.0 : speedsG) << (Swift.min(tabbark.count, 3))))")
         break
      }
         tabbarq.append("\(1 & placey.count)")
         meal4 = "\((tabbark == (String(cString:[121,0], encoding: .utf8)!) ? meal4.count : tabbark.count))"
      scroll6 = contQ.hasSuffix("\(scroll6)")
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(0.01, 0.01)
   while (contQ.count > 4) {
      contQ.append("\((contQ == (String(cString:[70,0], encoding: .utf8)!) ? (scroll6 ? 3 : 3) : contQ.count))")
      break
   }
            self.alpha = 0.0
        }
    }

@discardableResult
 func failureComeInvalidDisappearBasic(placeholderRestore: Double, codelabelRemove: [String: Any]!) -> [String: Any]! {
    var loadg: String! = String(cString: [97,95,54,55,95,100,97,112,0], encoding: .utf8)!
    var relationS: String! = String(cString: [97,98,115,108,95,118,95,52,56,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &relationS) { pointer in
          _ = pointer.pointee
   }
    var configurationd: [String: Any]! = [String(cString: [97,98,99,115,0], encoding: .utf8)!:854, String(cString: [104,105,103,104,108,105,103,104,116,105,110,103,95,119,95,52,53,0], encoding: .utf8)!:858]
      relationS.append("\(relationS.count % (Swift.max(1, loadg.count)))")
      configurationd[loadg] = (loadg == (String(cString:[101,0], encoding: .utf8)!) ? relationS.count : loadg.count)
      relationS.append("\(((String(cString:[57,0], encoding: .utf8)!) == loadg ? configurationd.count : loadg.count))")
      relationS = "\(((String(cString:[103,0], encoding: .utf8)!) == loadg ? configurationd.count : loadg.count))"
      loadg.append("\(relationS.count << (Swift.min(labs(3), 3)))")
       var sortA: String! = String(cString: [99,95,50,54,95,101,110,115,117,114,101,0], encoding: .utf8)!
       _ = sortA
       var topO: String! = String(cString: [108,97,114,103,101,114,0], encoding: .utf8)!
      if sortA.count >= 5 {
         sortA = "\(topO.count << (Swift.min(3, sortA.count)))"
      }
         topO = "\(topO.count)"
         topO = "\(topO.count | 1)"
      repeat {
         sortA = "\(((String(cString:[70,0], encoding: .utf8)!) == topO ? sortA.count : topO.count))"
         if sortA.count == 3513719 {
            break
         }
      } while (sortA.count == 3513719) && (topO == String(cString:[71,0], encoding: .utf8)!)
          var l_titleA: String! = String(cString: [116,101,120,116,115,0], encoding: .utf8)!
          var speakU: Double = 5.0
          var type_13b: String! = String(cString: [112,111,112,117,112,95,97,95,52,0], encoding: .utf8)!
         topO.append("\(topO.count & type_13b.count)")
         l_titleA = "\(((String(cString:[80,0], encoding: .utf8)!) == sortA ? sortA.count : topO.count))"
         speakU /= Swift.max(4, Double(type_13b.count))
      for _ in 0 ..< 1 {
         topO = "\(topO.count << (Swift.min(4, sortA.count)))"
      }
      configurationd[loadg] = loadg.count
   return configurationd

}






    override func awakeFromNib() {

         let canopusFindasoc: [String: Any]! = failureComeInvalidDisappearBasic(placeholderRestore:6159.0, codelabelRemove:[String(cString: [120,95,51,50,95,115,101,110,100,109,115,103,0], encoding: .utf8)!:937, String(cString: [105,110,118,105,116,101,95,114,95,53,51,0], encoding: .utf8)!:855, String(cString: [116,114,97,99,101,95,56,95,50,49,0], encoding: .utf8)!:660])

      let canopusFindasoc_len = canopusFindasoc.count
     var tmp_g_9 = Int(canopusFindasoc_len)
     tmp_g_9 *= 40
      canopusFindasoc.forEach({ (key, value) in
          print(key)
          print(value)
      })

_ = canopusFindasoc


       var constraintB: String! = String(cString: [116,95,57,51,95,112,114,101,115,101,116,115,0], encoding: .utf8)!
    var socketq: [Any]! = [580, 129]
   repeat {
       var speedsG: String! = String(cString: [105,110,102,111,114,109,97,116,105,118,101,0], encoding: .utf8)!
       var aymentE: String! = String(cString: [116,101,120,105,100,101,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &aymentE) { pointer in
    
      }
          var rawingY: Double = 4.0
          var modity1: String! = String(cString: [121,95,50,53,95,114,101,115,116,0], encoding: .utf8)!
         speedsG.append("\(modity1.count + 3)")
         rawingY += (Double((String(cString:[74,0], encoding: .utf8)!) == modity1 ? speedsG.count : modity1.count))
      if speedsG.hasSuffix("\(aymentE.count)") {
         speedsG = "\(speedsG.count - 2)"
      }
      if speedsG.count == 1 {
          var flow3: Int = 1
          var finishD: String! = String(cString: [116,111,110,103,117,101,0], encoding: .utf8)!
          var configO: String! = String(cString: [102,108,97,99,0], encoding: .utf8)!
          var secondsj: String! = String(cString: [101,120,112,108,111,114,101,114,0], encoding: .utf8)!
         aymentE.append("\((finishD == (String(cString:[67,0], encoding: .utf8)!) ? aymentE.count : finishD.count))")
         flow3 >>= Swift.min(aymentE.count, 1)
         configO.append("\(speedsG.count)")
         secondsj.append("\(2 >> (Swift.min(2, secondsj.count)))")
      }
      repeat {
         speedsG.append("\(aymentE.count)")
         if 4337149 == speedsG.count {
            break
         }
      } while (aymentE.count > speedsG.count) && (4337149 == speedsG.count)
         speedsG.append("\(speedsG.count + 1)")
      for _ in 0 ..< 3 {
         speedsG.append("\(speedsG.count)")
      }
      constraintB = "\(constraintB.count)"
      if constraintB.count == 2000307 {
         break
      }
   } while (socketq.count >= 2) && (constraintB.count == 2000307)
      socketq.append(constraintB.count & socketq.count)

      constraintB = "\(socketq.count)"
        super.awakeFromNib()
        
    }

@discardableResult
 func memberLikePurchaseLoad(islookDefalut: Int, epairNum: Int) -> Bool {
    var handleD: Float = 4.0
    _ = handleD
    var descH: Double = 3.0
    var data9: Bool = false
   while (data9 && 5.70 > (handleD / 5.74)) {
      data9 = !data9
      break
   }
   for _ in 0 ..< 3 {
      data9 = descH > 6.53 && 6.53 > handleD
   }
      descH += (Double(Int(handleD > 251462628.0 || handleD < -251462628.0 ? 17.0 : handleD) & (data9 ? 1 : 1)))
   for _ in 0 ..< 2 {
      descH -= (Double(Int(descH > 331994944.0 || descH < -331994944.0 ? 71.0 : descH) * Int(handleD > 268936196.0 || handleD < -268936196.0 ? 7.0 : handleD)))
   }
   repeat {
      descH *= (Double(1 + Int(handleD > 183197673.0 || handleD < -183197673.0 ? 57.0 : handleD)))
      if descH == 1134210.0 {
         break
      }
   } while (descH == 1134210.0) && (handleD >= Float(descH))
   for _ in 0 ..< 3 {
      descH /= Swift.max((Double(2 * Int(handleD > 349944004.0 || handleD < -349944004.0 ? 24.0 : handleD))), 5)
   }
   return data9

}





    
    
    @IBAction func close(_ sender: Any) {

         let demuxerPlanarx: Bool = memberLikePurchaseLoad(islookDefalut:2435, epairNum:2041)

      if demuxerPlanarx {
      }

_ = demuxerPlanarx


       var window_pc4: Int = 4
    var selectedy: Double = 5.0
    _ = selectedy
    var showM: [Any]! = [String(cString: [114,97,112,112,101,114,0], encoding: .utf8)!, String(cString: [99,111,108,111,117,114,0], encoding: .utf8)!]
   if 2 > window_pc4 {
      window_pc4 %= Swift.max(window_pc4 - 3, 1)
   }

   repeat {
       var fontW: Float = 1.0
       var timebuttonL: String! = String(cString: [114,111,119,115,112,97,110,0], encoding: .utf8)!
       var normalc: String! = String(cString: [100,111,119,110,115,116,114,101,97,109,0], encoding: .utf8)!
       var basici: Double = 0.0
       var myloadingL: Bool = false
         fontW += (Float(Int(fontW > 334922900.0 || fontW < -334922900.0 ? 63.0 : fontW) + 2))
         basici -= (Double(Int(basici > 112884582.0 || basici < -112884582.0 ? 23.0 : basici) / 3))
         fontW /= Swift.max(Float(normalc.count >> (Swift.min(labs(3), 4))), 4)
      for _ in 0 ..< 1 {
          var sectionL: Int = 3
          var titlesV: Double = 0.0
          var leanU: String! = String(cString: [101,116,104,101,114,115,99,97,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &leanU) { pointer in
    
         }
         fontW *= Float(normalc.count)
         sectionL /= Swift.max(3, (Int(titlesV > 349169391.0 || titlesV < -349169391.0 ? 12.0 : titlesV)))
         titlesV /= Swift.max(3, Double(timebuttonL.count))
         leanU.append("\(((myloadingL ? 3 : 2) / (Swift.max(3, Int(titlesV > 104814803.0 || titlesV < -104814803.0 ? 30.0 : titlesV)))))")
      }
      while (!myloadingL) {
         myloadingL = normalc.contains("\(basici)")
         break
      }
         normalc.append("\(normalc.count)")
      while (1 <= timebuttonL.count) {
          var selectedV: Float = 1.0
          var o_centerQ: [String: Any]! = [String(cString: [112,114,101,118,101,110,116,115,0], encoding: .utf8)!:104, String(cString: [105,109,105,113,0], encoding: .utf8)!:744, String(cString: [108,111,103,111,117,114,108,0], encoding: .utf8)!:951]
          var accountlabely: Bool = false
          var navgationn: Double = 0.0
         timebuttonL = "\(timebuttonL.count)"
         selectedV += (Float((myloadingL ? 5 : 2) | Int(fontW > 57958687.0 || fontW < -57958687.0 ? 66.0 : fontW)))
         o_centerQ["\(navgationn)"] = (1 / (Swift.max(Int(navgationn > 354999694.0 || navgationn < -354999694.0 ? 54.0 : navgationn), 4)))
         accountlabely = 12.12 == fontW && myloadingL
         break
      }
         timebuttonL = "\((3 + Int(fontW > 327763215.0 || fontW < -327763215.0 ? 38.0 : fontW)))"
      if !myloadingL {
         myloadingL = !myloadingL
      }
       var frame_n70: String! = String(cString: [117,98,108,105,99,0], encoding: .utf8)!
          var placeholderM: Double = 5.0
          var digitM: String! = String(cString: [121,95,57,55,95,98,114,111,119,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &digitM) { pointer in
                _ = pointer.pointee
         }
         normalc = "\((normalc.count | Int(fontW > 231351036.0 || fontW < -231351036.0 ? 57.0 : fontW)))"
         placeholderM -= (Double(Int(fontW > 188399659.0 || fontW < -188399659.0 ? 60.0 : fontW) ^ 2))
         digitM = "\(3 ^ normalc.count)"
         timebuttonL.append("\(timebuttonL.count)")
          var downloadW: String! = String(cString: [115,99,97,108,101,102,97,99,116,111,114,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &downloadW) { pointer in
    
         }
          var chooseA: String! = String(cString: [101,95,52,54,95,109,112,115,117,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &chooseA) { pointer in
                _ = pointer.pointee
         }
          var orderZ: String! = String(cString: [99,100,97,116,97,0], encoding: .utf8)!
          _ = orderZ
         normalc = "\(1)"
         downloadW = "\(timebuttonL.count)"
         chooseA = "\(timebuttonL.count * 2)"
         orderZ = "\(((myloadingL ? 5 : 1)))"
      for _ in 0 ..< 1 {
         basici -= (Double(Int(basici > 58572451.0 || basici < -58572451.0 ? 32.0 : basici) & (myloadingL ? 5 : 2)))
      }
      repeat {
         timebuttonL.append("\((Int(basici > 284407508.0 || basici < -284407508.0 ? 74.0 : basici) | Int(fontW > 18978151.0 || fontW < -18978151.0 ? 7.0 : fontW)))")
         if 3025820 == timebuttonL.count {
            break
         }
      } while (3.24 > (fontW + Float(timebuttonL.count)) || 5.100 > (fontW + 3.24)) && (3025820 == timebuttonL.count)
         frame_n70.append("\(frame_n70.count)")
      selectedy -= (Double(Int(basici > 117650400.0 || basici < -117650400.0 ? 5.0 : basici) | (myloadingL ? 5 : 2)))
      if selectedy == 447170.0 {
         break
      }
   } while (selectedy == 447170.0) && (4.61 == (selectedy + 2.71) || (2.71 + selectedy) == 2.43)
        transform = CGAffineTransformMakeScale(1.0, 1.0)
      selectedy *= Double(1)
        alpha = 1.0
      showM.append((Int(selectedy > 350311934.0 || selectedy < -350311934.0 ? 27.0 : selectedy) | 3))
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(0.01, 0.01)
   while ((selectedy / 2.16) <= 3.52) {
      window_pc4 <<= Swift.min(3, labs((Int(selectedy > 340376362.0 || selectedy < -340376362.0 ? 42.0 : selectedy))))
      break
   }
            self.alpha = 0.0
        }
      selectedy /= Swift.max(Double(showM.count), 3)
    }

    
    func showCardView() {
       var deletebuttonk: Int = 1
    var commentS: String! = String(cString: [118,95,54,53,95,114,101,105,109,112,111,114,116,0], encoding: .utf8)!
    _ = commentS
    var ditu: String! = String(cString: [104,111,116,105,122,111,110,116,108,0], encoding: .utf8)!
   while (commentS.hasPrefix("\(ditu.count)")) {
      ditu = "\(commentS.count % (Swift.max(7, ditu.count)))"
      break
   }

      deletebuttonk *= 2
        transform = CGAffineTransformMakeScale(0.01, 0.01)
      ditu.append("\(2)")
        alpha = 0.0
       var homes: String! = String(cString: [100,101,109,117,120,101,114,0], encoding: .utf8)!
       var resulte: String! = String(cString: [102,108,105,103,104,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &resulte) { pointer in
    
      }
       var target2: [Any]! = [313, 649, 157]
         resulte = "\(2 >> (Swift.min(5, homes.count)))"
      if target2.count < resulte.count {
         target2 = [target2.count]
      }
         resulte = "\(homes.count)"
       var a_objectw: Bool = false
         a_objectw = homes.count >= 42
      repeat {
         homes.append("\(target2.count)")
         if 542219 == homes.count {
            break
         }
      } while (542219 == homes.count) && (resulte.hasSuffix(homes))
         homes = "\(1 + resulte.count)"
         homes = "\(resulte.count)"
      if !resulte.hasPrefix("\(a_objectw)") {
         a_objectw = (31 == (target2.count * (a_objectw ? target2.count : 31)))
      }
      commentS = "\(commentS.count >> (Swift.min(5, labs(deletebuttonk))))"
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(1.0, 1.0)
   while ((3 >> (Swift.min(1, labs(deletebuttonk)))) < 5 || (3 >> (Swift.min(1, commentS.count))) < 2) {
      deletebuttonk %= Swift.max(4, ditu.count * commentS.count)
      break
   }
            self.alpha = 1.0
        }
      ditu.append("\(deletebuttonk / 2)")
    }
    
}

