
import Foundation

import UIKit

class WYCLayoutCell: UICollectionViewCell {
var electiOffset: Double = 0.0
var relationSelected_str: String!




    @IBOutlet weak var label: UILabel!
    @IBOutlet weak var icon: UIImageView!
    @IBOutlet weak var cell_Image: UIImageView!

@discardableResult
 func minimumConstantCloseAmplitudeObserverMaterialButton() -> UIButton! {
    var silenceC: Double = 4.0
    var img7: Bool = true
      silenceC /= Swift.max((Double((img7 ? 1 : 3) | Int(silenceC > 359161325.0 || silenceC < -359161325.0 ? 45.0 : silenceC))), 4)
      img7 = 10.85 < silenceC
       var gesturep: [Any]! = [235, 634, 328]
       var assitantL: Bool = false
      for _ in 0 ..< 2 {
          var creationo: String! = String(cString: [122,95,51,54,95,114,109,115,116,114,101,97,109,0], encoding: .utf8)!
          _ = creationo
         assitantL = gesturep.count == 41
         creationo = "\(gesturep.count ^ 2)"
      }
      for _ in 0 ..< 2 {
         assitantL = !assitantL
      }
      while (4 >= (gesturep.count & 5)) {
         gesturep = [2]
         break
      }
         assitantL = gesturep.count > 94
          var zhidingesW: String! = String(cString: [111,119,110,101,114,115,104,105,112,0], encoding: .utf8)!
          var replaceM: Float = 2.0
          var playB: Double = 0.0
         withUnsafeMutablePointer(to: &playB) { pointer in
                _ = pointer.pointee
         }
         assitantL = !assitantL
         zhidingesW = "\((2 ^ Int(replaceM > 118812977.0 || replaceM < -118812977.0 ? 78.0 : replaceM)))"
         replaceM -= (Float(gesturep.count << (Swift.min(5, labs((assitantL ? 5 : 4))))))
         playB += (Double(Int(replaceM > 307052043.0 || replaceM < -307052043.0 ? 70.0 : replaceM)))
         assitantL = (53 >= (gesturep.count * (!assitantL ? gesturep.count : 53)))
      silenceC *= Double(gesturep.count)
       var end6: Int = 1
       var ylabel1: String! = String(cString: [99,111,112,121,104,95,105,95,57,52,0], encoding: .utf8)!
       _ = ylabel1
         end6 >>= Swift.min(2, labs(2))
      for _ in 0 ..< 2 {
         end6 -= ylabel1.count << (Swift.min(labs(2), 1))
      }
         end6 <<= Swift.min(3, ylabel1.count)
         end6 |= ylabel1.count
         end6 %= Swift.max(ylabel1.count << (Swift.min(2, labs(end6))), 5)
         end6 *= 3
      img7 = ylabel1 == (String(cString:[65,0], encoding: .utf8)!)
     let rootWrite: UIView! = UIView()
     var frame_gSpeech: String! = String(cString: [105,118,97,114,115,95,118,95,51,52,0], encoding: .utf8)!
     let repairApp: UIImageView! = UIImageView()
    var providerRecurse:UIButton! = UIButton(frame:CGRect.zero)
    rootWrite.frame = CGRect(x: 303, y: 15, width: 0, height: 0)
    rootWrite.alpha = 0.9;
    rootWrite.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var rootWriteFrame = rootWrite.frame
    rootWriteFrame.size = CGSize(width: 255, height: 248)
    rootWrite.frame = rootWriteFrame
    if rootWrite.isHidden {
         rootWrite.isHidden = false
    }
    if rootWrite.alpha > 0.0 {
         rootWrite.alpha = 0.0
    }
    if !rootWrite.isUserInteractionEnabled {
         rootWrite.isUserInteractionEnabled = true
    }

    repairApp.frame = CGRect(x: 111, y: 13, width: 0, height: 0)
    repairApp.alpha = 0.5;
    repairApp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    repairApp.animationRepeatCount = 3
    repairApp.image = UIImage(named:String(cString: [97,116,116,114,105,98,117,116,101,115,0], encoding: .utf8)!)
    repairApp.contentMode = .scaleAspectFit
    
    var repairAppFrame = repairApp.frame
    repairAppFrame.size = CGSize(width: 267, height: 188)
    repairApp.frame = repairAppFrame
    if repairApp.isHidden {
         repairApp.isHidden = false
    }
    if repairApp.alpha > 0.0 {
         repairApp.alpha = 0.0
    }
    if !repairApp.isUserInteractionEnabled {
         repairApp.isUserInteractionEnabled = true
    }

    providerRecurse.alpha = 0.2;
    providerRecurse.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    providerRecurse.frame = CGRect(x: 83, y: 57, width: 0, height: 0)
    providerRecurse.setTitle("", for: .normal)
    providerRecurse.setBackgroundImage(UIImage(named:String(cString: [112,104,111,116,111,0], encoding: .utf8)!), for: .normal)
    providerRecurse.titleLabel?.font = UIFont.systemFont(ofSize:15)
    providerRecurse.setImage(UIImage(named:String(cString: [105,116,101,109,0], encoding: .utf8)!), for: .normal)

    
    var providerRecurseFrame = providerRecurse.frame
    providerRecurseFrame.size = CGSize(width: 246, height: 279)
    providerRecurse.frame = providerRecurseFrame
    if providerRecurse.alpha > 0.0 {
         providerRecurse.alpha = 0.0
    }
    if providerRecurse.isHidden {
         providerRecurse.isHidden = false
    }
    if !providerRecurse.isUserInteractionEnabled {
         providerRecurse.isUserInteractionEnabled = true
    }

    return providerRecurse

}





    
    override func awakeFromNib() {

         var prefetcherSbpro: UIButton! = minimumConstantCloseAmplitudeObserverMaterialButton()

      if prefetcherSbpro != nil {
          self.addSubview(prefetcherSbpro)
          let prefetcherSbpro_tag = prefetcherSbpro.tag
     var temp_q_81 = Int(prefetcherSbpro_tag)
     temp_q_81 += 43
      }

withUnsafeMutablePointer(to: &prefetcherSbpro) { pointer in
        _ = pointer.pointee
}


       var strokeY: [String: Any]! = [String(cString: [114,101,102,108,101,99,116,111,114,95,51,95,56,55,0], encoding: .utf8)!:931, String(cString: [109,111,110,105,116,111,114,0], encoding: .utf8)!:995, String(cString: [103,95,56,56,95,102,105,108,101,115,0], encoding: .utf8)!:776]
    var json5: Bool = false
   withUnsafeMutablePointer(to: &json5) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
       var information7: Double = 3.0
       _ = information7
       var refreshf: [Any]! = [143, 511, 501]
       _ = refreshf
       var commentw: Double = 1.0
          var timersF: [Any]! = [499, 193, 738]
         information7 /= Swift.max(5, Double(2 + timersF.count))
       var lengthd: String! = String(cString: [114,101,102,105,110,101,100,0], encoding: .utf8)!
      while (information7 < 2.12) {
         information7 *= (Double(Int(commentw > 368136708.0 || commentw < -368136708.0 ? 40.0 : commentw) | refreshf.count))
         break
      }
      while ((refreshf.count << (Swift.min(labs(1), 2))) >= 3 || 1 >= (refreshf.count - Int(information7 > 33986346.0 || information7 < -33986346.0 ? 15.0 : information7))) {
         refreshf.append(refreshf.count | 1)
         break
      }
      while (4.63 >= (information7 - Double(lengthd.count)) && 3.89 >= (information7 - 4.63)) {
         information7 += (Double(lengthd.count ^ Int(commentw > 16010320.0 || commentw < -16010320.0 ? 44.0 : commentw)))
         break
      }
         commentw += Double(refreshf.count)
         lengthd = "\((Int(information7 > 93386828.0 || information7 < -93386828.0 ? 58.0 : information7)))"
       var downloadA: String! = String(cString: [114,97,116,101,115,0], encoding: .utf8)!
       var offsetO: String! = String(cString: [110,97,112,115,104,111,116,0], encoding: .utf8)!
       _ = offsetO
         offsetO = "\((offsetO.count >> (Swift.min(5, labs(Int(information7 > 19040102.0 || information7 < -19040102.0 ? 47.0 : information7))))))"
         downloadA.append("\(1 % (Swift.max(2, refreshf.count)))")
      strokeY["\(refreshf.count)"] = 3 & refreshf.count
   }
      strokeY = ["\(strokeY.count)": (strokeY.count << (Swift.min(5, labs((json5 ? 3 : 2)))))]

   for _ in 0 ..< 2 {
      json5 = (44 >= (strokeY.values.count ^ (json5 ? 44 : strokeY.values.count)))
   }
       var interfaceC: Double = 4.0
       var materialG: Bool = false
       _ = materialG
       var idss: String! = String(cString: [115,99,116,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &idss) { pointer in
             _ = pointer.pointee
      }
      if 3.54 <= interfaceC {
         materialG = !idss.hasSuffix("\(materialG)")
      }
       var shous: [String: Any]! = [String(cString: [107,101,121,0], encoding: .utf8)!:5549.0]
      withUnsafeMutablePointer(to: &shous) { pointer in
    
      }
      repeat {
          var draws: String! = String(cString: [102,105,110,110,101,121,0], encoding: .utf8)!
          var namesn: Double = 4.0
          var sublyout1: Double = 5.0
          var playingu: String! = String(cString: [102,105,102,111,115,95,98,95,55,57,0], encoding: .utf8)!
          _ = playingu
         idss = "\((Int(interfaceC > 379267027.0 || interfaceC < -379267027.0 ? 24.0 : interfaceC) ^ (materialG ? 2 : 3)))"
         draws = "\((Int(namesn > 265537237.0 || namesn < -265537237.0 ? 79.0 : namesn)))"
         sublyout1 += (Double(idss.count / (Swift.max(4, Int(namesn > 7326035.0 || namesn < -7326035.0 ? 35.0 : namesn)))))
         playingu.append("\((2 * Int(interfaceC > 251477316.0 || interfaceC < -251477316.0 ? 11.0 : interfaceC)))")
         if (String(cString:[95,109,99,121,99,0], encoding: .utf8)!) == idss {
            break
         }
      } while ((String(cString:[95,109,99,121,99,0], encoding: .utf8)!) == idss) && (interfaceC <= 2.32)
         materialG = interfaceC == 31.64
      while (2 >= (idss.count % 2) || (shous.keys.count % (Swift.max(idss.count, 1))) >= 2) {
         idss.append("\(shous.values.count)")
         break
      }
      for _ in 0 ..< 3 {
          var indexN: Int = 0
          _ = indexN
         interfaceC += Double(shous.values.count ^ 2)
         indexN |= 1
      }
      while (3.96 >= interfaceC) {
         interfaceC /= Swift.max(Double(idss.count), 4)
         break
      }
         materialG = 53 > shous.keys.count
      repeat {
          var mineK: Bool = true
         idss = "\((Int(interfaceC > 196710433.0 || interfaceC < -196710433.0 ? 95.0 : interfaceC)))"
         mineK = 8.2 > interfaceC
         if (String(cString:[108,57,49,107,50,114,120,101,0], encoding: .utf8)!) == idss {
            break
         }
      } while (shous.count < idss.count) && ((String(cString:[108,57,49,107,50,114,120,101,0], encoding: .utf8)!) == idss)
      strokeY = ["\(json5)": (1 << (Swift.min(labs(Int(interfaceC > 249372267.0 || interfaceC < -249372267.0 ? 44.0 : interfaceC)), 4)))]
        super.awakeFromNib()
    
    }

}
